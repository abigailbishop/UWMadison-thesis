\chapter{Data Cleaning for the Five Station Analysis}

  The data taken by the Askaryan Radio Array have been analyzed a few times for neutrinos, solar flares, and to deduce the properties of the ice at the South Pole. 
  When the livetime of the five stations is added together, there are roughly 28 station-years of data but only 9 years of that data have been analyzed.
  This is partly because each station takes so long to calibrate and prepare for analysis that only some data was ready for analysis when previous analyses were conducted but also because there is so much data to clean before one can perform an analysis. 
  For example, 8 of the station years that have been analyzed were analyzed by a team lead by two graduate students. 
  ARA currently has 5 graduate students, 2 postdoctoral researchers, and a faculty member who are devoted to analyzing the full 28 station-years of ARA data. 
  This process involves building an analysis pipeline from the ground up, cleaning years of data, and simulating neutrino and noise events then viewing how many are removed through the cleaning pipeline, and analyzing remaining events for neutrino signals. 
  
\section{Simulation Setup for Data Cleaning}

  It is important to consider how each cut translates to overall data loss and expected neutrino rejection. 
  Neutrino events are therefore simulated uniformly in log( energy ) space with energies from $10^{16}$ eV to $10^{21}$ eV. 
  As in Chapter 4 Section 4, neutrinos are thrown in a forward simulation along trajectories that intersect with a cylinder 15~km in radius and 3~km in depth. % Fix this reference, use variables
  These neutrinos interact randomly within the Earth and all outgoing particles with energy greater than $10^{16}$ eV are simulated further. 
  Muon and tau stochastic losses, tau decays, and outgoing neutrino interactions are simulated. 
  
  Noise simulations are also generated using the spectral coefficients calculated from the "software trigger" events that are read out once per second, regardless of sufficient signal to trigger the detector. % Check the frequency of these events
  \begin{figure}
    \centering
    \includegraphics[
      width=0.7\textwidth,
      alt={  
        Line plot of spectral coefficients as a function of frequency for vertically and horizontally polarized antennas on the four strings of A2 in configuration 7 compared to the previously used model. 
        The old model is larger than the new models and there are differences between the horizontally and vertically polarized antennas, motivating the per-channel noise modelling. 
      }
      ]{sections/5sa-cleaning/images/noisemodel_sigmavsfreq_A_2_config_7}
    \caption{Spectral coefficients for the }
    \label{fig:noisemodel}
  \end{figure}

  \begin{figure}
    \centering
    \begin{subfigure}[t]{0.48\textwidth}
      \centering
      \includegraphics[
        width=\textwidth,
        alt={
          Line plot showing voltage as a function of time for a thermal-like RF event (an event that triggered the detector). 
          The line varies stochastically between -50 volts and 50 volts, characteristic of thermal-like recordings.
        }
        ]{sections/5sa-cleaning/images/event_rf_station_2_run_19291_eventNo_6573_7_trace.pdf} 
        % python plot_event.py --station_id 2 --run_number 19291 --event_number 6573 --data_type data --channel 7
    \end{subfigure}
    ~
    \begin{subfigure}[t]{0.48\textwidth}
      \centering
      \includegraphics[
        width=\textwidth,
        alt={
          Line plot showing voltage as a function of time for a simulated noise event. 
          The line varies stochastically between -50 volts and 50 volts, characteristic of thermal-like recordings.
        }
        ]{sections/5sa-cleaning/images/event_noise_station_2_noise_config_7_run_2700000001_eventNo_101_7_trace.pdf}
        % python plot_event.py --station_id 2 --run_number 2700000001 --event_index 101 --data_type noise --channel 7 --config 7 # From 260213 sims
    \end{subfigure}
    \caption{Example of a thermal-like waveform in channel 7 (a bottom vertically polarized antenna) in A2 configuration 7 from data (left) and simulation (right).}
    \label{fig:noiseevent}
  \end{figure}
  
  \begin{figure}
    \centering
    \includegraphics[
      width=0.75\textwidth,
      alt={
        Line plot showing voltage as a function of time for a software triggered event. 
        The line varies stochastically between -50 volts and 50 volts, characteristic of thermal-like recordings.
      }
    ]{sections/5sa-cleaning/images/event_soft_station_2_run_19291_eventNo_6268_7_trace.pdf}
    % python plot_event.py --station_id 2 --run_number 19291 --event_number 6268 --data_type data --channel 7
    \caption{Example of a software triggered event showing mostly thermal-like data from an A2 configuration 7 event: Run 19291 Event 6288.}
    \label{}
  \end{figure}
  
  The traces of the signal simulations and noise simulations are fed through the cleaning pipeline so that, as cuts are applied to remove backgrounds from the dataset, we can ensure that we are doing so in a way that maximized the potential residual neutrino event population. 
  At the end of data cleaning, the goal is that our data distributions for key analysis variables resemble the noise simulation distributions such that any outliers that may arise are due to neutrino events. 
  
\section{Defining Livetime Configurations for A2 \label{sec:a2configs}}

  The behavior of detector triggering and readout changes dramatically as the detector's configuration is changed. 
  This has serious effects on event reconstruction and analysis, so each station's livetime is divided into livetime configurations. 
  These livetime configurations are mostly based on antenna/string masking, trigger window, and the amount of waveform readout ahead of the bin that triggered the detector, called the pre-trigger readout window or the number of pre-RF blocks. 
  Each trigger block is 10~ns and the total trigger window is defined as 
  \begin{equation}
    TW = (N_{TB}+1) * 10~\text{ns}
    \label{eq:trigwin}
  \end{equation}
  and the default trigger window is 10 blocks meaning 110~ns.
  Each RF block is 20~ns and the default setting for pre-RF blocks is 10, so the default pre-trigger readout window is 200~ns. 
  As a station needs three like-polarization antennas to trigger, there may be signal recorded one or more antennas in advance of the bin that triggered so it's important to tune the number of pre-RF blocks accordingly. 
  Having too few pre-RF blocks yielded poor reconstruction of calibration pulser events in configuration 6 of station 2 after String 1 was masked and was noticed in June 2020 on an Operations Call and adjusted. 

  \missingfigure{Reconstructed calpulser vs unixtime to show deterioration in C6}
  
  Figure~\ref{fig:configs} shows the evolution of these key parameters for the full station 2 livetime along with shaded regions indicating each defined livetime configuration. 
  What is not pictured in this figure is the masking of String 1 of Station 2 which began on run 15527 in January of 2020. 
  Note that these configurations have changed since the 4 year survey of Stations 2 and 3. 
  That analysis placed emphasis on the masked antennas, trigger window, presence of trigger delays, and full readout window length. 
  This analysis has discovered that the latter 2 settings are less impactful when defining run configurations compared to the antenna masking, trigger window, and recently appreciated pre-trigger readout window. 
  Further considering each new livetime configuration requires a distinct set of noise, signal, and effective volume simulations that take well over a hundred thousand CPU-hours to generate, livetime configuration permutations are minimized by only considering the trigger window, pre-trigger readout window, and the status of antenna masking. 
  
  \begin{figure}
    \centering
    \includegraphics[
      width=0.75\textwidth,
      alt={
        Three vertically stacked plots showing the number of blocks in a RF-triggered waveform, the number of pre-RF blocks used to center a waveform, and the number of blocks in a software-triggered waveform as a function of run number. 
        Colored backgrounds for the three plots distinguish the 7 A2 livetime configurations.
        The colored backgrounds line up with many changes in RF trigger block and pre-RF trigger block settings.
      }
    ]{sections/5sa-cleaning/images/a2_2013_2021_woBadRuns_blk_info.png}
    \caption{
      The number of RF Trigger blocks (waveform length for RF events), the number of Pre-RF Trigger Blocks (amount of time before the triggering block to include in RF event read out, affects where the trigger block lays in the waveform), and the number of Software Trigger blocks (waveform length of software triggered events) as a function of run number extracted from A2 configuration files.
      The differently colored backgrounds of the plot delineate the 7 different configurations in A2. 
      The number of pre-RF blocks are an important setting driving station livetime configurations.
      This figure does not include run configuration data from known bad runs as some have extreme values for special calibration runs.
    }
    \label{fig:configs}
  \end{figure}

  % This overflows the hbox
  % \begin{table}
  %   \centering
  %   \begin{tabular}{|c|c|c|c|c|c|c|c|}
  %     \hline
  %     Run Configuration & Dates & Runs & Livetime [days] & Trigger Window [ns] & Pre-Readout Window [ns] & Hardware + Analysis Masks & Trigger Mask \\
  %     \hline
  %     1 & 2/18/13 - 2/12/14 & 0 - 3036 & 239.00 & 2 & Default (10) &  & D4BH \\
  %     \hline
  %   \end{tabular}
  %   \caption{x}
  %   \label{tab:}
  % \end{table}
  
  \begin{figure}
    \centering
    \includegraphics[
      width=0.99\textwidth,
      alt={
        A2 livetime configurations parameters.
      }
    ]{sections/5sa-cleaning/images/a2configs.png}
    \caption{
      Table with the driving characteristics of the 7 A2 livetime configurations. 
    }
    \label{tab:configs}
  \end{figure}
  
\section{Using the AraProc Analysis Pipeline}

  AraProc\footnote{\url{https://github.com/ara-software/AraProc/tree/main}} is a python-based framework spearheaded by Marco Muzio and Brian Clark that reads in L1 ARA data and converts it into condensed L2 data.
  The root-formatted L1 data include raw waveforms that take up roughly 7 GB of space per full run. 
  The root-formatted L2 data is created using the L1 data but only includes simple metadata and analysis variables describing each event, so full runs of L2 data only take up roughly 6 MB of space. 
  The preexisting metadata saved includes the time of an event, the trigger type, and the detector configuration among other settings. 
  The calculated analysis variables generated by AraProc include the reconstructed azimuth, zenith, and signal correlation for nearby "pulser" maps and distant 300m maps for each polarization (horizontal and vertical) as well as direct and refracted rays for the distant maps. 
  The radius of the pulser map used for the reconstruction is uniquely set to the physical distance from a station's pulser to the station's center.
  Other analysis variables include the SNR of the coherently summed waveform for an event, the average RMS of the waveforms, the impulsivity, and the spark power ratios. 
  Some of these analysis variables will be explained in more detail in the coming sections as they are used to employ various cuts. 
  The AraProc framework can be run on computing clusters so many runs may be processed in parallel. 

  \subsection{Continuous Waveform Filtering}
  
\section{Removing Spoiled Data}

  The first genre of cuts that will be discussed are those that remove bad events from the data. 
  These events are usually the result of digitizing malfunctions. 
  
  \subsection{1st Minute Cut}
  
    During the first minute of a run, events from ARA stations are typically corrupted as the waveforms recorded usually have a large offset compared to the pedestal values of the waveform. 
    This is the result of unstable power draw in the station as it starts a new run. 
    Due to this large offset, it is expected that the average waveform RMS and SNR are abnormal during the first minute of a run compared to the remainder of the run. 
    Shown in Figure~\ref{fig:firstmin_waveform} are the waveforms in every ARA2 channel for an event in the first minute of a run. 
    Figure~\ref{fig:firstmin_2d} shows how the average waveform RMS changes in the first 10 minutes of a run as well. 
    The impulsive nature of these waveforms can mimic the neutrino signal this analysis is targeting so these events must be removed from the analysis. 
    
    
    \begin{figure}
      \centering
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}
        ]{sections/5sa-cleaning/images/firstmin_station_2_run_17627_eventNo_2_trace.png}
      \end{subfigure}
      ~
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}
        ]{sections/5sa-cleaning/images/firstmin_station_2_run_17627_eventNo_2_spectrum.png}
      \end{subfigure}
      \caption{Example waveform (left) and spectrum (right) for an event taken within the first minute of a new run, motivating the removal of all events within the first minute of a run. This is the second event of run 17,627 of ARA2.}
      \label{fig:firstmin_waveform}
    \end{figure}
    
    % SNR or RMS vs unix time for the start of the run is not that telling. I don't remember which variables showed that the first minute of runs were bad but I'll have to remind myself
    % Myoungchul's wiki says its ADC. I'm not sure if we have anything related to ADC saved in L2 data
    % So I might have to go back to L1 data for this
    \missingfigure{2D plot showing ADC vs unixtime for the first 1 hour of a run. \label{fig:firstmin_2d}}
    
  \subsection{Spark Events}
  
    Another class of detector glitch backgrounds is called the Spark Event. 
    These events occur when one of the digitizing boards on the station's mainboard withstands a surge of current that inflates the digitized signal.
    Each digitizing board reads out one string for each ARA station so if one string has noticeably inflated RMS compared to the other strings, a spark event has likely taken place.
    The waveform and spectrum of an example spark event is shown in Figure~\ref{fig:spark_waveform}
    
    \begin{figure}
      \centering
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}
        ]{sections/5sa-cleaning/images/spark_station_2_run_7100_eventNo_1467_trace.png}
      \end{subfigure}
      ~
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}
        ]{sections/5sa-cleaning/images/spark_station_2_run_7100_eventNo_1467_spectrum.png}
      \end{subfigure}
      \caption{Example waveform (left) and spectrum (right) for a spark event. Notice the significant waveform inflation in Channel 0, 4, 8, 12 (all from the same string, sharing a digitizing board) and the flattened spectrum that result from the digitizing board malfunction. This example is event 1467 of run 7100 in ARA2.}
      \label{fig:spark_waveform}
    \end{figure}

    To systematically identify these events, the ratio in average waveform power is taken between the two strings with the greatest average power consumption and events with non-thermal, so-called, \texttt{spark\_power\_ratio}s are removed.
    What is classified as a non-thermal \texttt{spark\_power\_ratio} is determined by fitting an exponential function to the tail of the distribution of \texttt{spark\_power\_ratio}s for all events in a station's configuration that aren't removed by the Bad Run Cut or the First Minute Cut.
    The non-thermal \texttt{spark\_power\_ratio} cut is set where the integral beyond the exponential tail fit is 1. 
    An example of the \texttt{spark\_power\_ratio} distribution, tail fit, and cut is shown in Figure~\ref{fig:spr}

    \begin{figure}
      \centering
      \includegraphics[
          width=0.6\textwidth,
          alt={}
      ]{sections/5sa-cleaning/images/A2_c7_spr.png}
      \caption{The distribution of \texttt{spark\_power\_ratio}s for events in A2 configuration 7, the exponential function fit to the distribution's high-value tail, and cut established where the integral under the fit is equal to 1.}
      \label{fig:spr}
    \end{figure}

  \subsection{Anomalous Channel Cut}
    
    \begin{figure}
      \centering
      \includegraphics[
          width=0.6\textwidth,
          alt={}
      ]{sections/5sa-cleaning/images/s4r3733e40469.png}
      \caption{Event 40469 from Station 4 run 3733 demonstrating excessive recorded power in the bottom antennas of string 3.}
      \label{fig:a4outlier}
    \end{figure}

    There are occasions where a channel pair reads out a waveform with more power than all other strings and antennas. 
    Event 40469 from run 3733 recorded by Station 4 inspired this cut as the impulsive, as shown in Figure~\ref{fig:a4outlier}.
    This event is reminiscent of 3 similar events from A2 that passed all cuts in the A2+A3 4 year analysis: Run 3439 event 19142, run 6290 event 29583, and run 3689 event 87466 \cite{ara_a23}. % https://aradocs.wipac.wisc.edu/cgi-bin/DocDB/ShowDocument?docid=2032 slide 5
    
    The anomalous channel cut is designed to remove events where one channel's waveform has an unusually high amount of power and does not correlate well with waveforms from other antennas of the same polarization.
    For the former, we calculate the \texttt{max\_rms\_ratio} by calculating the root-mean-square of the waveform recorded from all channels with like-polarization, find the max, and divide it by the average.
    For the latter, we calculate the \texttt{max\_rms\_corr} by calculating the root-mean-square of all pair-wise cross-correlations between all channels of like-polarization, finding the maximum and identifying the maximum.
    The bad run cut, the first minute cut, and the spark event cuts are then applied to the full burn sample, resulting in a thermal-like dataset of events. 
    From this dataset, distributions of \texttt{max\_rms\_ratio} and \texttt{max\_rms\_corr} for both vertically and horizontally polarized antennas are composed.
    The tails on these four distributions are then fit to an exponential tail, the start of which is automatically adjusted to optimize the reduced $\chi^2$ of the fit.
    The cut on each polarization of \texttt{max\_rms\_ratio} and \texttt{max\_rms\_corr} is established as the point at which the integral under the tail's fit is equal to 1. 
    Fits to these four variables are performed for each configuration on each station. 
    An example of the fits from A2 configuration 7 are shown in Figure~\ref{fig:acc}

    \begin{figure}
      \centering
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}  
        ]{sections/5sa-cleaning/images/a2c7_max_rms_corr_v.png}
      \end{subfigure}
      ~
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}
        ]{sections/5sa-cleaning/images/a2c7_max_rms_corr_h.png}
      \end{subfigure}
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}
        ]{sections/5sa-cleaning/images/a2c7_max_rms_ratio_v.png}
      \end{subfigure}
      ~
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}
        ]{sections/5sa-cleaning/images/a2c7_max_rms_ratio_h.png}
      \end{subfigure}
      \caption{Distributions, tail fits, and cuts established for the variables used in the Anomalous Channel Cut for A2 Configuration 7.}
      \label{fig:acc}
    \end{figure}

  \subsection{Low Reconstruction Confidence Cut \label{sec:lrcc}}

    The last cut designed to remove spoiled data is one that removes signal-like events that do not reconstruct well. 
    This cut defines signal like events as those with non-thermally high average signal-to-noise ratios in like-polarization antennas, non-thermal maximum correlation between waveforms from pairs of like-polarization antennas, and non-thermal impulsivity significance (the significance of the hypothesis test between a linear fit versus an error response function plus linear fit to the impulsivity curve, following Wilks’ theorem). 
    A poorly reconstructing event is identified as those with non-thermally low correlation contrast, which is calculated from the skymap of pair-wise correlation values by dividing the difference between the maximum value and average value by the average value across the whole map.
    An example of an event with low correlation contrast is shown in Figure~\ref{fig:lowcorrcontrast} and examples of fits to the four aforementioned variables are shown in Figure~\ref{fig:lrcc}.
    
    \begin{figure}
      \centering
      \includegraphics[
          width=0.75\textwidth,
          alt={}
      ]{sections/5sa-cleaning/images/a4_lrcc.png}
      \caption{An example of an event with a non-thermally low correlation contrast.}
      \label{fig:lowcorrcontrast}
    \end{figure}

    \begin{figure}
      \centering
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}
        ]{sections/5sa-cleaning/images/a2c7_avg_snr_v.png}
      \end{subfigure}
      ~
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}
        ]{sections/5sa-cleaning/images/a2c7_global_max_corr_v.png}
      \end{subfigure}
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}
        ]{sections/5sa-cleaning/images/a2c7_impSig_v.png}
      \end{subfigure}
      ~
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
            width=\textwidth,
            alt={}
        ]{sections/5sa-cleaning/images/a2c7_corr_contrast_v.png}
      \end{subfigure}
      \caption{Distributions, tail fits, and cuts established for the variables used in the Low Reconstruction Confidence Cut for A2 Configuration 7.}
      \label{fig:lrcc}
    \end{figure}

\section{Removing Anthropogenic and Science Backgrounds}

  After all erroneously digitized events are removed, events that include backgrounds we do not want to misclassify as neutrino signals must be removed. 
  This includes mis-tagged calibration pulser events, runs with known anthropogenic backgrounds, and potential cosmic ray events. 

  \subsection{Geometry Cut}
  
    The geometry cut is programmed to catch calibration pulser events that are not tagged as calibration pulser events. 
    % State how often this happens
    This may occur because the calibration pulser on a station is programmed to run once per second but it is the station's job to trigger naturally on the signals from the pulser. % Check the 1 per second bit
    The station records when the calibration pulser is firing and logs an event as a calibration event if the station triggers while the pulser is live. 
    Sometimes, calibration pulser events are not tagged as such and slip into the science dataset.
    For ARA2, events like these appear in the windows of reconstructed zenith and azimuth described in Table~\ref{tab:A2GC} on a configuration-by-configuration basis.
    The Geometry Cut enforces that events that fall within these windows are removed entirely from the analysis to remove unwanted pulser contamination from our samples.  
    
    The signals from calibration pulsers are usually recorded with strong pulses, making the interferometric reconstruction of the calpulser event obvious. 
    However pulser signals can shift in such a way that secondary solutions for the reconstruction overtake the correct solution. 
    This gives rise to populations of reconstructed calibration pulser events that are called "mirrors". 
  
    \begin{figure}
      \centering
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
          width=\textwidth,
          alt={
            16 panels, one for each channel in A2, displaying the recorded voltage as a function of time for this calibration pulser event.
          }
          ]{sections/5sa-cleaning/images/event_cp_station_2_run_19291_eventNo_6365_trace.pdf} 
          % python plot_event.py --station_id 2 --run_number 19291 --event_number 6365 --data_type data
      \end{subfigure}
      ~
      \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[
          width=\textwidth,
          alt={
            2-dimensional heat map for the correlation scores of a calibration pulser event with arriving from particular azimuth (y-axis) and elevation (x-axis) angles. 
            The highest correlation score is near the marker for CP1, the expected location of one of A2's calibration pulsers.
          }
          ]{sections/5sa-cleaning/images/event_cp_station_2_run_19291_eventNo_6365_map_pulser_v.pdf} 
          % python plot_skymap.py --station_id 2 --run_number 19291 --event_number 6365 --data_type data --map_type pulser_v
      \end{subfigure}
      \caption{Example of a calibration pulser event recorded by A2 in Run 19291 as Event 6365. The left figure shows the waveforms recorded in all 16 antennas and the right figure shows a reconstruction skymap that displays the event's correlation score with the ansatz of arriving from all points on the $4\pi$ sky, evenly binned in elevation and azimuth.}
      \label{fig:noiseevent}
    \end{figure}

    \missingfigure{phi vs theta for all CP events with annotations pointing out CP5, CP5 mirror, and CP6. Something like \texttt{/home/abishop/ara/FiveStation/scripts/L2/plots/a2/cp\_cut/cp5\_brc\_nmc\_avgSNRv5\_2d\_reco\_result\_pulser\_v\_theta\_vs\_reco\_result\_pulser\_v\_phi\_cp.png}}

    \begin{figure}
      \centering
      \includegraphics[
        width=0.75\textwidth,
        alt={x}
      ]{sections/5sa-cleaning/images/a2cp5regions.png}
      \caption{x}
      \label{fig:cp_region_cartoon}
    \end{figure}
    
    \begin{table}
      \centering
      \begin{tabular}{ | c | c c | c c |}
        \hline
        Object Being Cut & $\theta (^{\circ})$ & $\Delta\theta (^{\circ})$ & $\phi (^{\circ})$ & $\Delta\phi (^{\circ})$ \\
        \hline
        CP 5V & -23.5 & 4.5 & -26.35 & 4.7 \\
        CP 5V Mirror 1 & 32.45 & 5.3 & -25.55 & 3.8 \\
        CP 5V Mirror 2 & 32.45 & 5.3 & -42.1 & 3.75 \\
        CP 5V Mirror 3 & 32.45 & 5.3 & -42.1 & 3.75 \\
        CP 6V & 4.5 & 5.95 & 62.9 & 6.25 \\
        \hline
      \end{tabular}
      \caption{Fitted calibration pulser reconstruction regions for ARA2.}
      \label{tab:A2GC}
    \end{table}
    
    \begin{table}
      \centering
      \begin{tabular}{ | c | c |}
        \hline
        Run Range & Firing Calibration Pulser \\
        \hline
        0-1900 & 6 \\
        1901-1934 & 5 \\
        1935-7005 & 6 \\
        7006 - 8097 & 5 \\
        8098 - 9578 & 6 \\
        9779 - 19826 & 5 \\
        \hline
      \end{tabular}
      \caption{Run periods and which calibration pulser was active during those periods.}
      \label{tab:A2GC}
    \end{table}
    
    To ensure that all potential untagged calibration pulser events are captured within the geometry cut, a gaussian kernel density equation is fit over the distribution of reconstructed zenith and azimuth of these events. 
    The gaussian kernel density equation was chosen by Brian Clark as the most appropriate for our data. 
    This allows us to model the tails of the reconstructed arrival directions to ensure no untagged calibration pulser events arise in our full data sample.
    
    \begin{figure}
      \centering
      \includegraphics[
        width=0.9\textwidth,
        alt={x}
      ]{sections/5sa-cleaning/images/cp5_brc-nmc-sec_fit_251210.png}
      \caption{CP5 Fit}
      \label{fig:cp5fit}
    \end{figure}

    \begin{figure}
      \centering
      \includegraphics[
        width=0.9\textwidth,
        alt={x}
      ]{sections/5sa-cleaning/images/cp5m1_brc-nmc-sec_fit_251210.png}
      \caption{CP5 Mirror 1 Fit}
      \label{fig:cp5m1fit}
    \end{figure}

    \begin{figure}
      \centering
      \includegraphics[
        width=0.9\textwidth,
        alt={x}
      ]{sections/5sa-cleaning/images/cp5m2_brc-nmc-sec_fit_251210.png}
      \caption{CP5 Mirror 2 Fit}
      \label{fig:cp5m2fit}
    \end{figure}

    \begin{figure}
      \centering
      \includegraphics[
        width=0.9\textwidth,
        alt={x}
      ]{sections/5sa-cleaning/images/cp6_brc-nmc-sec_fit_251210.png}
      \caption{CP6 Fit}
      \label{fig:cp6fit}
    \end{figure}

    % Should I show a plot motivating calpulser leakage? Or just cite somebody else

  \subsection{Bad Configuration Cut}
  
    Station configurations where the livetime of data, excluding bad runs, is less than 30 days are excluded from analysis. 
    Table~\ref{tab:badconfigs} shows the list of bad configurations for each station in the array. 
    Note that A2, the station focused on in this thesis, does not have any bad configurations so this cut is not applied in this thesis. 
    Due to challenges synchronizing the colocated A5 and PA instruments, A5 livetime recorded while the PA was recording are omitted to remove the possibility that duplicate events (events seen in both A5 and PA instruments) appear in the analysis. 
    Configuration 2 of A5 has more than 30 days of raw livetime, but after excluding data taken while the PA was running, A5 has less than 30 days of livetime and is therefore excluded from the analysis. 
    
    \begin{table}
      \centering
      \begin{tabular}{| c | c |}
        \hline
        Station & Bad Configuration \\
        \hline
        1 & 1 \\
        2 & None \\
        3 & None \\
        4 & 1 \\
        5 & 2 \\
        PA & 3 \\
        \hline
      \end{tabular}
      \caption{Every ARA station's livetime configurations excluded from the analysis via the Bad Run Cut due to a lack of sufficient livetime. }
    \end{table}

  \subsection{Bad Run Cut}
    
    The Bad Run Cut draws directly from the Bad Run Log for the appropriate ARA station. 
    Runs are typically added to the Bad Run Log if there are known anthropogenic backgrounds present during the run. 
    This includes, but is not limited to, runs that are known to include surface pulsing calibration campaigns and heavy station activity that cannot be cleaned simply (for example, a geometry cut cannot remove snowmobile backgrounds that move in reconstructed azimuth and zenith). 
    
    Some runs can be cut due to unusual voltages in the digitizing daughter boards. \todo{explain the DDA run cut}
    Others can be cut because the station's configuration is abnormal as discussed in Section~\ref{sec:a2configs}
    
    \missingfigure{}
    
    For runs that include a large portion of anthropogenic background, it is safer to remove the entire run than to add dozens of multi-dimensional and hyper-specific cuts to remove the data. 
    The livetime reduction is not significant in doing so. 
    Brian Clark created a pipeline that systematically identifies runs with an excessive amount of outliers. 
    This pipeline takes the global maximum correlation for each RF event, plots them on a histogram, and fits two gamma Gumbel curves to the histogram. 
    The Gumbel curve is defined as % Cite Gumbel
    \begin{equation}
      f(x) = A \frac{ \beta^{\alpha} }{ \sigma ~ \Gamma(\alpha) } e^{- ( \alpha \frac{x-\mu}{\sigma} + \beta e^{({-\frac{x-\mu}{\sigma} })} ) }
      \label{eq:gumbel}
    \end{equation}
    where $A$ is a scaling factor, $\alpha=\beta$ and is initialized to 1 but free to fit, $\sigma$ is initialized as the average between the lower and upper 1$\sigma$ boundaries of the curve is called the scale parameter, and $\mu$ is initialized as peak of the histogram and called the location parameter. 
    This distribution is "suitable for modelling highly skewed and heavy-tailed data" and fits our thermal backgrounds and outliers well.  % cite http://www.kurims.kyoto-u.ac.jp/EMIS/journals/AMEN/2020/AMEN-190321.pdf and their 1st source
    The first gamma Gumbel curve is fit to the thermal background and the second is fit to the remaining outliers. 
    Examples of the gamma Gumbel curve fits to a clean and contaminated run of A2 are shown in Figure~\ref{fig:gumbel-clean} and Figure~\ref{fig:gumbel-dirty}, respectively.
    The difference between the test statistics of the Poisson negative log-likelihoods for each dual component gamma Gumbel distribution is then calculated. 
    This $\Delta TS$ parameter characterizes the separation between populations of thermal backgrounds and outliers in correlation-space. 
    A low $\Delta TS$ implies low separation and that the run is not polluted with abnormally correlated outliers. 
    A high $\Delta TS$ implies the opposite, that many events in the run are outliers in the regime of global max correlation. 
    A histogram of the $\Delta TS$ values for each run in each station's livetime configuration is plotted. 
    A run-level cut based on the value of $\Delta TS$ for the run is determined based on the presence of runs with copious outliers compared to the distribution of runs that are predominately thermal as shown in Figure~\ref{fig:deltaTS-per-config}. 
    
    \missingfigure{Gumbel for a clean run in A2 \label{fig:gumbel-clean}}
    \missingfigure{Gubmel for an outlier-y run in A2 \label{fig:gumbel-dirty}}
    \missingfigure{Distribution of gumbel curves for each config of A2 \label{fig:deltaTS-per-config}}
    
    
  \subsection{Spatio-Temporal Cut}

    \begin{table}
      \centering
      \begin{tabular}{|c|c|c|c|c|c|}
        \hline
        Variable & \texttt{bImp\_v} & \texttt{corr\_contrast\_v} & \texttt{csw\_snr\_v} & \texttt{global\_max\_corr\_v} & \texttt{wavefront\_rms\_v} \\
        \hline
        Threshold type & greater & greater & greater & greater & less \\
        \hline 
        1 & 0.307 & 1.025 & 7.067 & 0.198 & 0.001 \\
        2 & 0.266 & 1.011 & 6.534 & 0.194 & 0.001 \\
        3 & 0.263 & 1.062 & 7.218 & 0.231 & 0.001 \\
        4 & 0.236 & 0.998 & 6.825 & 0.225 & 0.001 \\
        5 & 0.260 & 1.013 & 7.095 & 0.218 & 0.001 \\
        6 & 0.220 & 1.344 & 6.905 & 0.136 & 0.001 \\
        7 & 0.229 & 1.343 & 6.960 & 0.138 & 0.001 \\
        \hline
      \end{tabular}
      \caption{Spatio Temporal Cut signal-ness variables and thresholds}
      \label{tab:stc_variables}   
    \end{table}
  
    This cut identifies events that occur in quick succession and reconstruct to the same region of the sky. 
    Any anthropogenic background that is not removed by the Bad Run Cut (e.g. South Pole radio emissions, snowmobiles, planes) is likely to be removed by the spatio-temporal cut. 
    The cut finds clusters of events by first identifying events that exceed various signal-ness thresholds. 
    These thresholds are determined differently for each station. 
    For A2, these thresholds are based on the impulsivity variable \texttt{bImp\_v}, reconstruction variables \texttt{corr\_contrast\_v} and \texttt{global\_max\_corr\_v}, signal strength variable \texttt{cs\_snr\_v}, and plane wave characterization variable \texttt{wavefront\_rms\_v} as reported in Table~\ref{tab:stc_variables}.
    Once events with sufficient signal and correlation scores are identified, the time of each event is used to determine temporal clustering. 
    The largest time window, $t$ used to assemble spatio-temporal clusters is also different on each station, but for A2 it is 60~s. 
    Lastly the reconstructed azimuth and elevation are used to identify clusters in space. 
    The solid angle in which events must reside to be removed from this cut scales in size with $\log(t)$, meaning a shorter $t$ will correspond to larger clusters and vice versa as
    \begin{equation}
      \Omega = e^{ 4 \pi t  }
      \label{eq:stc_angularsize}
    \end{equation}
    where $t$ is at least 1 second and at most 1 year.

  \subsection{Zenith Cut and Surface-Like Cut \label{sec:zc_slc}}
  
    The Zenith Cut is employed at the end of the cleaning pipeline to remove any remaining anthropogenic surface events, such as continuous wave contaminated events that weren't fully filtered, and cosmic ray events. 
    The cosmic ray events that ARA can observe typically result from an ultra high energy cosmic ray (UHECR) interacting in the atmosphere, causing an airshower, and potentially impacting into the ice. 
    Depending on the energy and direction the UHECR is traveling in, an ARA station may see radio signals from the air shower portion and/or the ice-impacting core. 
    % Cite Nicolas, maybe show photos
    The Zenith Cut is harsh and strict and is employed last so that other cuts can be tuned as much as possible to remove backgrounds. 
    Also note, that while traditionally refered to as the Zenith Cut, the cut is actually imposed on an events reconstructed elevation.

    Continuous Wave contamination interferes with the reconstruction of a signal and can therefore make a surface event reconstruct in the ice. 
    Relying on the zenith cut to remove all surface related backgrounds is risky for this reason and should be employed near the end of cleaning. 
    
    The surface-like cut is designed to remove events that are signal-like but have a high probability of originating from the surface. 
    An event is determined as signal-like is the same as it was defined in Section~\ref{sec:lrcc}: if an event has non-thermally high average SNR, maximum waveform correlation score, and impulsivity significance. 
    The variable \texttt{mindepth} characterizes the highest zenith an event may have come from and is used to determine if an event probably originated the surface. 
    This variable is calculated using the map of correlation values for all locations on the sky in azimuth and zenith, as shown in Figure~\ref{fig:lowmindepth}.
    The highest zenith in the reconstruction map with a correlation value equal or greater to 60\% of the maximimum correlation value in the whole map is the \texttt{mindeth} in degrees of elevation.
    We take this one step further and use Fresnel's law to convert this \texttt{mindepth} into units of meters depth from the surface. 
    This is used to catch any events that were not cut by the Low Reconstruction Confidence Cut or the traditional Zenith Cut. 

    \begin{figure}
      \centering
      \includegraphics[
          width=0.75\textwidth,
          alt={}
      ]{sections/5sa-cleaning/images/a1r3183e65990_mindepth.png}
      \caption{An example of an event with a high \texttt{mindepth\_v}, meaning this event has many possible reconstructions that imply this event originated near the surface. This example is A1, run 3183, event 65990.}
      \label{fig:lowmindepth}
    \end{figure}

\section{Quick Removal of Thermal-Like Events during 100\% Processing with the Fast Filter}

  As every event in a run takes 1-2 seconds to be processed and there are typically $\mathcal{O}(100,000)$ events per full 100\% file, we designed a so-called Fast Filter based on two computationally efficient analysis variables that can distinguish well between thermal-like and signal-like events. 
  This Fast Filter is a Linear Discriminant Analysis (LDA) trained using the unblinded 10\% data sample and simulated neutrinos labeled for the LDA as thermal-like and signal-like, respectively, with all cuts applied to both datasets.
  The LDA is composed of four variables: \texttt{wavefront\_rms} and \texttt{avg\_s2} for each polarization of antennas. 

  \texttt{wavefront\_rms} is a measure of how compatible observed waveforms are with an arriving plane wave. 
  We use a simplified version of what was originally proposed by Carl Pfender and explained in Brian Clark's thesis.
  For like-polarization antenna, we determine the time of maximum peak-to-peak (maximum minus minimum) voltage in any 20 nanosecond window over the whole waveform.
  We then assume, for the purpose of this calculation, that the signal from a potential plane wave arrived at this antenna at this time of maximum peak-to-peak voltage separation.
  For each pair of antennas, the time difference between these maximum peak-to-peak times is used to calculate the arrival angle of the signal as
  \begin{equation}
    cos(\theta) = \frac{c \Delta t}{n \Delta d}
    \label{eq:wavefrontrms}
  \end{equation}
  where $c$ is the speed of light in a vacuum, $\Delta t$ is the time delay between antennas, $n$ is the refractive index of the medium (ice), and $\Delta d$ is the distance between the two antennas. 
  When comparing the arrival angle calculated by each pair of antennas, if a plane wave was observed then these arrival angles should all be similar. 
  If a thermal-like event was observed, then these calculated arrival angles should all be quite different from each other. 
  To quantify this, we calculate the variance of all the calculated arrival angles (contrary to the canonical variable name of ``RMS'').
  When this variance, the \texttt{wavefront\_rms}, is small, the recorded waveform has a strong likelihood of originating from a plane wave and when it is large, this scenario is less likely.

  \texttt{avg\_s2} is a measure of signal causality constructed similarly to the \texttt{wavefront\_rms}. 
  For all antennas of like-polarization, the time of maximum peak-to-peak voltage in any 20 nanosecond window of every waveform is identified. 
  Again assuming that a plane wave arrived at each antenna at the determined time of maximal voltage separation, for each pair of antennas we calculate the average spacetime interval as
  \begin{equation}
    s^2 = \Delta r^2 - ({c}{n})^2 \Delta t^2
    \label{eq:avg_s2}
  \end{equation}
  where $\Delta r$ is the distance between a pair of antennas, $c$ is the speed of light in a vacuum, $n$ is the index of refraction of the medium, and  $\Delta t$ is the time difference between the assumed signal arrival in each antenna (the time of maximal voltage separation).
  When $s^2$ is positive this indicates a causal relationship between signals in each channel. 
  When $s^2$ is negative, this indicates an acausal, non-physical signal that would not be expected of an arriving impulsive signal.
  We take the average $s^2$ calculated over all antenna pairs of like-polarization and report this as the \texttt{avg\_s2}

  The training of the Fast Filter's linear discriminant is then performed over both of these variables and for both polarizations of antennas (4 variables in total).
  An example of the result of this training is shown in Figure~\ref{fig:fastfilter_LDA} for A2 Configuration 7.
  We choose a Fast Filter cut such that 90\% of simulated neutrinos would be preserved and for A2 Configuration 7 this results in a removal of over 97\% of thermal-like events in the dataset.

  \begin{figure}
    \centering
    \includegraphics[
        width=0.75\textwidth,
        alt={}
    ]{sections/5sa-cleaning/images/a2c7_fastfilter.png}
    \caption{Results of the Fast Filter's linear discriminant analysis trained on simulated neutrino data (blue, Signal line) and thermal-like data events (red, Background line).}
    \label{fig:fastfilter_LDA}
  \end{figure}

  We learned, after the unblinding proposal when we were motivated to profile our L2 processing code after it was taking longer than expected, that the main computational bottleneck was not the generation of reconstruction skymaps built by cross-correlating waveforms 64,800 times but actually in performing the continuous waveform (CW) removal via the algorithm SineSubtract.
  In future analyses we recommend attempting to speed up CW removal as opposed to identifying events to skip during L2 processing.
  
\section{Employment of Cuts and Impact on Potential Neutrino Events}

  The cuts are applied to A2 data in the following order: 
  \begin{enumerate}
    \item Fast Filter (100\% sample only)
    \item Bad Run and Bad Configuration Cut
    \item First Minute Cut
    \item Spark Event Cut
    \item Anomalous Channel Cut
    \item Low Reconstruction Confidence Cut
    \item Geometry and Known Sources Cut
    \item Spatio-Temporal Cut
    \item Zenith Cut
    \item Surface-Like Cut
  \end{enumerate}

  \pagebreak

  \begin{figure}
    \centering
    \includegraphics[
        width=0.75\textwidth,
        alt={}
    ]{sections/5sa-cleaning/images/a2c7_nu_noCuts_2d_global_max_corr_v_vs_csw_snr_v_rf.png}
    \caption{SNR vs Correlation neutrinos before cuts}
    \label{fig:snrcorr_nu_nocuts}
  \end{figure}

  \begin{figure}
    \centering
    \includegraphics[
        width=0.75\textwidth,
        alt={}
    ]{sections/5sa-cleaning/images/a2c7_nu_allCuts_2d_global_max_corr_v_vs_csw_snr_v_rf.png}
    \caption{SNR vs Correlation neutrinos after all cuts}
    \label{fig:snrcorr_nu_allcuts}
  \end{figure}

  \pagebreak

  \begin{figure}
    \centering
    \includegraphics[
        width=0.75\textwidth,
        alt={}
    ]{sections/5sa-cleaning/images/a2c7_10pct_noCuts_2d_global_max_corr_v_vs_csw_snr_v_rf.png}
    % This was generated Jan 11, 2026
    \caption{SNR vs correlation for thermal-like background before cuts}
    \label{fig:snrcorr_data_nocuts}
  \end{figure}

  \begin{figure}
    \centering
    \includegraphics[
        width=0.75\textwidth,
        alt={}
    ]{sections/5sa-cleaning/images/a2c7_10pct_allCuts_2d_global_max_corr_v_vs_csw_snr_v_rf.png}
    % This was generated Jan 11, 2026
    \caption{SNR vs correlation for thermal-like background after all cuts}
    \label{fig:snrcorr_data_allcuts}
  \end{figure}

  \pagebreak

  \begin{figure}
    \centering
    \includegraphics[
        width=\textwidth,
        alt={}
    ]{sections/5sa-cleaning/images/inefficiencies_a2c7_data.png}
    \caption{Background rejection rate}
    \label{fig:brj}
  \end{figure}

  \begin{figure}
    \centering
    \includegraphics[
        width=\textwidth,
        alt={}
    ]{sections/5sa-cleaning/images/inefficiencies_a2c7_nu.png}
    \caption{signal efficiency}
    \label{fig:sigeff}
  \end{figure}

  \pagebreak

  \begin{figure}
    \centering
    \includegraphics[
        width=0.75\textwidth,
        alt={}
    ]{sections/5sa-cleaning/images/analysis_efficiencies_station2_config7_filtered_with_min_surface_energy.png}
    \caption{Neutrino signal efficiency vs energy for each cut}
    \label{fig:sigeff_energy}
  \end{figure}

  \begin{figure}
    \centering
    \includegraphics[
        width=0.75\textwidth,
        alt={}
    ]{sections/5sa-cleaning/images/analysis_efficiencies_station2_config7_filtered_with_min_surface_snr.png}
    \caption{Neutrino signal efficiency vs SNR for each cut}
    \label{fig:sigeff_snr}
  \end{figure}
  